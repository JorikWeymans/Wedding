export class TimeUnitModel
{
    constructor(value, max, description, animationDelay)
    {
        this.value = value;
        this.max = max;
        this.description = description;
        this.animationDelay = animationDelay;
    }
}
